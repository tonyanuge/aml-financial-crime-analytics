# AML Analytics Source Code

Reusable Python functions will be moved here once notebook logic becomes stable enough to reuse or test.

Do not create abstraction for its own sake: notebook-first during exploration, then refactor proven logic.
