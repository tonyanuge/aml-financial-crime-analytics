# Data

Use synthetic or openly licensed training data only. Do not commit confidential, customer, employer or production transaction data.

Suggested later datasets:
- PaySim
- IBM AMLSim
- SAML-D
- mock sanctions / PEP data suitable for training

Raw files belong in `data/raw/`; transformed analytical outputs belong in `data/processed/`.
